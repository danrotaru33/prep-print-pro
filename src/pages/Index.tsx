
import MainDashboard from "./MainDashboard";

/**
 * Index: Only renders the main dashboard; all logic/state/UI is in MainDashboard.
 */
const Index = () => <MainDashboard />;

export default Index;
